{
  "name": "gforce-clean",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    {
  "scripts": {
    "prebuild": "echo 'Commit:' && git rev-parse HEAD && echo 'Branch:' && git rev-parse --abbrev-ref HEAD && echo '--- App.jsx (first 140 lines) ---' && sed -n '1,140p' src/App.jsx && echo '--- grep for gas-station-before-and-after ---' && (grep -Rni --include='*.{js,jsx,ts,tsx,css,json,md}' 'gas-station-before-and-after' . || true)",
    "build": "vite build"
  }
}

    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "@stripe/react-stripe-js": "^2.7.3",
    "@stripe/stripe-js": "^3.5.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-photo-album": "^2.3.1",
    "yet-another-react-lightbox": "^3.16.3",
    "stripe": "^16.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.4.0"
  }
}

